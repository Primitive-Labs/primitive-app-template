# Test environment configuration
# Deploy with: pnpm deploy --env test

# Set to your Primitive APP ID (can be a separate test app or same as production)
VITE_APP_ID=YOUR_APP_ID_GOES_HERE

# Which Primitive Servers to use (likely shouldn't change)
VITE_API_URL=https://primitiveapi.com
VITE_WS_URL=wss://primitiveapi.com

# Your configured OAuth Redirect URI for the test environment
VITE_OAUTH_REDIRECT_URI=https://[YOUR TEST URL]/oauth/callback

# Enable/disable the OAuth Proxy URL. Typically enabled for deployed environments.
VITE_ENABLE_AUTH_PROXY="true"

# Log level for test environment (can be more verbose than production)
VITE_LOG_LEVEL="debug"
